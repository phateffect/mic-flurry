diff --git a/src/corebluetooth/adapter.rs b/src/corebluetooth/adapter.rs
index 0c489a2..5397def 100644
--- a/src/corebluetooth/adapter.rs
+++ b/src/corebluetooth/adapter.rs
@@ -98,6 +98,38 @@ impl Adapter {
             sender: adapter_sender,
         })
     }
+
+    /// Retrieves peripherals already connected to macOS that expose all requested services.
+    pub async fn connected_peripherals_with_services(
+        &self,
+        service_uuids: &[uuid::Uuid],
+    ) -> Result<Vec<Peripheral>> {
+        let future = CoreBluetoothReplyFuture::default();
+        self.sender
+            .to_owned()
+            .send(CoreBluetoothMessage::RetrieveConnectedPeripherals {
+                service_uuids: service_uuids.to_vec(),
+                future: future.get_state_clone(),
+            })
+            .await?;
+        let CoreBluetoothReply::PeripheralsRetrieved(identifiers) = future.await else {
+            return Err(Error::UnexpectedCallback);
+        };
+        let mut peripherals = Vec::with_capacity(identifiers.len());
+        for identifier in identifiers {
+            let id = PeripheralId::from(identifier);
+            let mut retrieved = None;
+            for _ in 0..100 {
+                if let Ok(peripheral) = self.peripheral(&id).await {
+                    retrieved = Some(peripheral);
+                    break;
+                }
+                tokio::time::sleep(std::time::Duration::from_millis(10)).await;
+            }
+            peripherals.push(retrieved.ok_or(Error::DeviceNotFound)?);
+        }
+        Ok(peripherals)
+    }
 }

 #[async_trait]
diff --git a/src/corebluetooth/internal.rs b/src/corebluetooth/internal.rs
index c0f42aa..03a4707 100644
--- a/src/corebluetooth/internal.rs
+++ b/src/corebluetooth/internal.rs
@@ -24,7 +24,7 @@ use futures::select;
 use futures::sink::SinkExt;
 use futures::stream::{Fuse, StreamExt};
 use log::{error, trace, warn};
-use objc2::{ClassType, msg_send_id};
+use objc2::{ClassType, msg_send_id, runtime::ProtocolObject};
 use objc2::{rc::Retained, runtime::AnyObject};
 use objc2_core_bluetooth::{
     CBCentralManager, CBCentralManagerScanOptionAllowDuplicatesKey, CBCharacteristic,
@@ -156,6 +156,7 @@ struct PendingWriteWithoutResponse {
 #[derive(Clone, Debug)]
 pub enum CoreBluetoothReply {
     AdapterState(CBManagerState),
+    PeripheralsRetrieved(Vec<Uuid>),
     ReadResult(Vec<u8>),
     ReadRssi(i16),
     Connected,
@@ -168,6 +169,7 @@ pub enum CoreBluetoothReply {
 #[derive(Debug)]
 pub enum PeripheralEventInternal {
     Disconnected,
+    Mtu(u16),
     Notification(Uuid, Uuid, Vec<u8>),
     ManufacturerData(u16, Vec<u8>, i16),
     ServiceData(HashMap<Uuid, Vec<u8>>, i16),
@@ -438,6 +439,10 @@ pub enum CoreBluetoothMessage {
         filter: ScanFilter,
     },
     StopScanning,
+    RetrieveConnectedPeripherals {
+        service_uuids: Vec<Uuid>,
+        future: CoreBluetoothReplyStateShared,
+    },
     ConnectDevice {
         peripheral_uuid: Uuid,
         future: CoreBluetoothReplyStateShared,
@@ -738,5 +744,14 @@ impl CoreBluetoothInternal {
                 .get_mut(&peripheral_uuid)
                 .expect("If we're here we should have an ID");
+            let maximum_write_length = unsafe {
+                peripheral.peripheral.maximumWriteValueLengthForType(
+                    CBCharacteristicWriteType::CBCharacteristicWriteWithoutResponse,
+                )
+            };
+            let att_mtu = u16::try_from(maximum_write_length.saturating_add(3)).unwrap_or(u16::MAX);
+            let _ = peripheral
+                .event_sender
+                .try_send(PeripheralEventInternal::Mtu(att_mtu));
             peripheral
                 .connected_future_state
                 .take()
@@ -1396,6 +1401,9 @@ impl CoreBluetoothInternal {
                     },
                     CoreBluetoothMessage::StartScanning{filter} => self.start_discovery(filter),
                     CoreBluetoothMessage::StopScanning => self.stop_discovery(),
+                    CoreBluetoothMessage::RetrieveConnectedPeripherals { service_uuids, future } => {
+                        self.retrieve_connected_peripherals(&service_uuids, future).await;
+                    }
                     CoreBluetoothMessage::ConnectDevice{peripheral_uuid, future} => {
                         trace!("got connectdevice msg!");
                         self.connect_peripheral(peripheral_uuid, future);
@@ -1448,6 +1456,48 @@ impl CoreBluetoothInternal {
         fut.lock()
             .unwrap()
             .set_reply(CoreBluetoothReply::AdapterState(state))
+    }
+
+    async fn retrieve_connected_peripherals(
+        &mut self,
+        service_uuids: &[Uuid],
+        fut: CoreBluetoothReplyStateShared,
+    ) {
+        let service_uuids = service_uuids
+            .iter()
+            .copied()
+            .map(uuid_to_cbuuid)
+            .collect::<Vec<_>>();
+        let service_refs = service_uuids.iter().map(Deref::deref).collect::<Vec<_>>();
+        let services = NSArray::from_slice(&service_refs);
+        let peripherals = unsafe {
+            self.manager
+                .retrieveConnectedPeripheralsWithServices(&services)
+        };
+        let mut identifiers = Vec::with_capacity(peripherals.len());
+        for peripheral in peripherals {
+            unsafe {
+                peripheral.setDelegate(Some(ProtocolObject::from_ref(&*self.delegate)));
+            }
+            let ns_identifier = unsafe { peripheral.identifier() };
+            let identifier = nsuuid_to_uuid(&ns_identifier);
+            let maximum_write_length = unsafe {
+                peripheral.maximumWriteValueLengthForType(
+                    CBCharacteristicWriteType::CBCharacteristicWriteWithoutResponse,
+                )
+            };
+            let att_mtu = u16::try_from(maximum_write_length.saturating_add(3)).unwrap_or(u16::MAX);
+            identifiers.push(identifier);
+            self.on_discovered_peripheral(peripheral, None).await;
+            if let Some(peripheral) = self.peripherals.get_mut(&identifier) {
+                let _ = peripheral
+                    .event_sender
+                    .try_send(PeripheralEventInternal::Mtu(att_mtu));
+            }
+        }
+        fut.lock()
+            .unwrap()
+            .set_reply(CoreBluetoothReply::PeripheralsRetrieved(identifiers));
     }

     fn start_discovery(&mut self, filter: ScanFilter) {
diff --git a/src/corebluetooth/peripheral.rs b/src/corebluetooth/peripheral.rs
index a077c7e..c4e583a 100644
--- a/src/corebluetooth/peripheral.rs
+++ b/src/corebluetooth/peripheral.rs
@@ -126,5 +126,8 @@ impl Peripheral {
             loop {
                 match event_receiver.next().await {
+                    Some(PeripheralEventInternal::Mtu(mtu)) => {
+                        shared.mtu.store(mtu, std::sync::atomic::Ordering::Relaxed);
+                    }
                     Some(PeripheralEventInternal::Notification(uuid, service_uuid, data)) => {
                         let notification = ValueNotification {
                             uuid,
